<h1>編集画面</h1>
<%= link_to '戻る', titles_path %>
<%= form_with(model: @title, local: true) do |f|%>
  <%= f.label :title, "タイトル"%>
  <%= f.text_field :title %>

  <%= f.submit "タイトルを編集" %>  
  <% end %>
