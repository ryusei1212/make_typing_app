<h1>新規作成画面</h1>
<div class="row mb-3">
  <div class="col-6">
  <%= render 'shared/error_messages', model: @title%>

    <%= form_with(model: @title, local: true) do |f|%> 
      <%= render 'form', f: f %>
       <%= f.submit "新規作成", id: 'submit', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
