<h1>新規作成画面</h1>

<%= form_with(model: @title, local: true) do |f|%>
  <%= render 'shared/error_messages', model: f.object%>
  <%= f.label :title, "タイトル"%>
  <%= f.text_field :title %>

  <%= f.submit "保存" %>  
  <% end %>

